import React from 'react';

import Root from './profile.styles';
import type { Props } from './profile.d';

const Profile = (_props: Props) => {
	return <Root>Here you can activate your passcode or else</Root>;
};

export default Profile;
