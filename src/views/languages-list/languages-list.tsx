import React from 'react';

import Root from './languages-list.styles';

import type { Props } from './languages-list.d';

const LanguagesList = (_props: Props) => {
	return <Root>Languages List Component</Root>;
};

export default LanguagesList;
